import styled from 'styled-components';

const MinButtonContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
`;

export { MinButtonContainer };
